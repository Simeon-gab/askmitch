import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The developer tools badge is useful while working on the app, but it
  // obscures controls in this intentionally fullscreen interface.
  devIndicators: false,
};

export default nextConfig;
